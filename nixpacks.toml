[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["cd template-nextjs && npm install"]

[phases.build]
cmds = ["cd template-nextjs && npm run build"]

[start]
cmd = "cd template-nextjs && npm start"
